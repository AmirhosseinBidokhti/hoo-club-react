import React from 'react';

import './dashboard.styles.scss';

const Dashboard = () => (
    <div>
        this the dashboard page which contain the dashboard menu which links us to other 
        components like user balance, profile, etc.
    </div>
);

export default Dashboard;

